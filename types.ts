
export enum Mode {
  CREATE = 'create',
  EDIT = 'edit',
}

export enum CreateFunction {
  FREE = 'free',
  STICKER = 'sticker',
  TEXT = 'text',
  COMIC = 'comic',
}

export enum EditFunction {
  ADD_REMOVE = 'add-remove',
  RETOUCH = 'retouch',
  STYLE = 'style',
  COMPOSE = 'compose',
}
